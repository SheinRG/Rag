"""
DocMind AI — Chunker
Text chunking and embedding logic (standalone, used by ingest.py).
"""

# Chunking and embedding logic is now inlined in ingest.py
# to avoid circular imports. This module is kept for compatibility.
